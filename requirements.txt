# I use these configuration on my server,
# but the code may still work fine on older version of the following packages
torch>=1.0.0
torchvision>=0.2.1
numpy>=1.16.4
scipy>=1.2.1
skimage>=0.15.0
pillow>=6.0.0
